# 🤝 Team Collaboration Guide ⚡

This is a private team repo. Keep things clean and follow these simple guidelines:

- **Branch from `dev`**:  
  ```bash
  git checkout dev
  git checkout -b feature/<your-feature>
  ```
- **Commit messages**:
  ```bash
  feat(scope): short description
  fix(scope): short description
  ```
  (scope = backend, frontend, admin, AI, etc.)
- **Push & PR**:
Push your branch to dev. PR review is optional for internal team.
- **Environment & setup**:
Run npm install and fill .env before starting the server.
- **Keep repo clean**:
Don’t commit node_modules or secrets.

---